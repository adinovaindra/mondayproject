
import { HStack, Button } from "@chakra-ui/react"

const Try = () => {
  return (
    <HStack>
      <Button bgColor={'whitesmoke'} >Click me</Button>
      <Button bgColor={'whitesmoke'}>Click me</Button>
    </HStack>
  )
}

export default Try